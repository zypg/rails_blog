<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(conversation,url: topic_conversations_path(@topic)) do |f| %>
      <% if conversation.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

          <ul>
          <% conversation.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%#= f.label :context %>
        <%= f.text_area :context , placeholder: 'input ...'%>
      </div>

      <div class="actions">
        <%= f.submit class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
